% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/proxy.R
\name{can_accept_write}
\alias{can_accept_write}
\title{Test whether a callable can accept a write value}
\usage{
can_accept_write(fn)
}
\arguments{
\item{fn}{A value to test.}
}
\value{
A length-1 logical.
}
\description{
Returns \code{TRUE} for any callable that can accept a positional argument
(\code{reactiveVal}, store leaf, \code{\link[=reactiveProxy]{reactiveProxy()}} with a setter, a primitive,
or a closure with at least one formal). Returns \code{FALSE} for read-only
callables (\code{reactive(...)}, a \verb{\\() expr} closure, a \code{reactiveProxy}
built with no \code{set}), and for non-callables.
}
\details{
Gate a write through this when you don't control whether the callable
the caller handed you is writable — a typical pattern in widget
wrappers (see \code{\link[=write_back]{write_back()}}), where a read-only \code{content} argument
should silently skip the write rather than error.
}
