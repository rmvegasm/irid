% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/widget.R
\name{write_back}
\alias{write_back}
\title{Build a write-back event handler for a widget wrapper}
\usage{
write_back(callable, field, then = NULL)
}
\arguments{
\item{callable}{A callable that will receive \code{e[[field]]} on write
(typically a \code{reactiveVal}, store leaf, or \code{reactiveProxy}), or
\code{NULL} for "no write target." With both \code{callable} and \code{then}
\code{NULL}, returns \code{NULL} so the wrapper's \verb{events =} entry collapses
to nothing (IridWidget drops NULL entries).}

\item{field}{The payload field name to read on each event.}

\item{then}{Optional side handler called after the write. 0-arg or
1-arg accepted.}
}
\value{
A 1-arg function suitable for an \code{\link[=IridWidget]{IridWidget()}} \verb{events =}
entry — or \code{NULL} if both \code{callable} and \code{then} are \code{NULL}.
}
\description{
Returns an event handler that:
\enumerate{
\item Writes \code{e[[field]]} to \code{callable} iff \code{can_accept_write(callable)}.
\item Calls the optional \code{then} handler after — \code{then()} for 0-arg,
\code{then(e)} for 1+-arg.
}
}
\details{
This is the canonical way a widget wrapper closes the round-trip on a
state-like prop. The widget's \verb{events =} entry becomes a one-liner:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{events = list(change = write_back(content, "content", then = onChange))
}\if{html}{\out{</div>}}

Read-only callables (\code{reactive(...)}, a \verb{\\() expr} closure) make the
write silently skip, but the event listener still fires — the
force-send-on-no-op path in mount echoes the canonical value back to
the client, which snaps the widget back. The wrapper writes nothing
extra to get this behavior.
}
