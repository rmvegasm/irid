% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mini_store.R
\name{make_slot_accessor}
\alias{make_slot_accessor}
\title{Scalar slot accessor for \code{Each} (positional and keyed)}
\usage{
make_slot_accessor(get_value, set_value, scope)
}
\arguments{
\item{get_value}{0-arg callable returning the slot's current value.}

\item{set_value}{1-arg function called with the new value on write.}

\item{scope}{Scope from \code{\link[=make_scope]{make_scope()}}; the propagating observer is
registered here for cleanup.}
}
\value{
A \code{reactiveProxy} that reads from the internal leaf and writes
through \code{set_value}.
}
\description{
Builds the scalar-item analogue of \code{\link[=make_mini_store]{make_mini_store()}} -- a callable
(\code{reactiveProxy}) over a single value held in the parent collection.
Reads route through an internal \code{reactiveVal} that is kept in sync with
\code{get_value()} by a propagating observer; writes route through
\code{set_value()} to the parent. The internal \code{reactiveVal} is what gives
fine-grained reactivity: when the parent list is patched somewhere
else, an unchanged slot's observers don't fire.
}
\keyword{internal}
