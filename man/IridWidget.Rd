% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/widget.R
\name{IridWidget}
\alias{IridWidget}
\title{Construct a widget — a wrapper for an arbitrary JavaScript library}
\usage{
IridWidget(
  name,
  props = list(),
  events = list(),
  deps = NULL,
  container = NULL,
  .event = NULL
)
}
\arguments{
\item{name}{Widget registry name, matching a JS-side
\code{irid.defineWidget("<name>", ...)} call. Required, non-empty
character scalar.}

\item{props}{Named list of inputs (server → client). Per-key dispatch
on \code{\link[=is.function]{is.function()}}: callable values become observers; non-callable
values ride in the init message as constants. \code{NULL} entries are
forwarded to JS as \code{null} (not dropped), so wrappers can declare
their full prop shape with optional slots
(\code{props = list(content = ..., cursor = cursor)} where \code{cursor} may
be \code{NULL}) and the JS factory sees a predictable, complete object.}

\item{events}{Named list of event handlers (client → server). Keys
are lowercase kebab-case event names (matching the web's
\code{CustomEvent} convention). Each value is a 0/1/2-arg handler, or
\code{NULL} — \code{NULL} entries are dropped, so wrappers can forward an
optional handler declaratively without conditional list-building:
\verb{events = list(change = ..., }cursor-changed\verb{ = onCursorChanged)}
skips the cursor registration when the caller didn't pass one.}

\item{deps}{Optional \code{html_dependency} or list of them. Required for
any widget whose JS isn't already loaded by some other means.}

\item{container}{Optional \code{shiny.tag} for the wrapper element.
Defaults to \code{tags$div()}. irid sets \code{id} and \code{data-irid-widget}
automatically.}

\item{.event}{Element-level event-timing config — same shape as on a
plain tag (\code{event_immediate()} / \code{event_throttle()} / \code{event_debounce()},
or a named list keyed by event). Applies to both widget events and
any DOM events on the container.}
}
\value{
A irid widget construct with class \code{irid_widget}.
}
\description{
\code{IridWidget()} is the third irid process-tags citizen (alongside
control-flow nodes and \code{Output}). It emits a container element plus an
init record that mount turns into an \code{irid-widget-init} custom message.
The client's \code{irid.defineWidget("<name>", factory)} registration is
looked up by \code{name} and called once per mount.
}
\details{
Reactive \code{props} open a one-way observer (server → client), routed
client-side to the widget's \code{update(key, value)} hook. Event
handlers in \code{events} go in the other direction: the widget JS calls
\code{send(event, payload)}, the payload arrives R-side through the
standard \code{irid-events} machinery (timing, sequence, stale-indicator
gating all work uniformly).

Per-widget round-trip wiring lives in the widget's R wrapper — see
\code{\link[=write_back]{write_back()}} for the canonical pattern.
}
